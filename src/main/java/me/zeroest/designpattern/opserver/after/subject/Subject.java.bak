package me.zeroest.designpattern.opserver.after.subject;

import me.zeroest.designpattern.opserver.after.observer.Observer;

public interface SubjectBak {
    void registerObserver(String subject, Observer observer);
    void unregisterObserver(String subject, Observer observer);
    void notifyObservers(String subject, String message);
}
